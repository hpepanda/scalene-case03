applications:
- name: demo-hp-db-data
  mem: 128M
  services:
    expense-api-mongo-db:
       type: mongodb
  hooks:
     post-staging:
     # create imgages folder in the shared filesystem
     - mkdir -p "$HELION_FILESYSTEM"/images
  stackato:
    ignores:
    - node_modules
  env:
    NODE_ENV: production
    IMAGE_SERVER_URL: http://expense-api.54.174.66.242.xip.io/image